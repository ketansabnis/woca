<section class="auth login-page">
  <div class="container no-pd">
    <div class="col-lg-5 col-xs-12 col-md-12 col-sm-12 no-pd">
      <div class="login-form-wrapper pt-5">
        <p> <strong>Reset Password</strong></p>

        <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), method: :post, html: { novalidate: false, class: 'validate-form', id: 'password_form' }) do |f| %>

          <%= f.input :login, autofocus: true, autocomplete: false, class: 'form-control' %>

          <div class="signin-btn col-lg-12 pl-0 pr-0 pt-3 pb-3">
            <%= f.submit "Send reset password instructions", class: 'sign-in-btn',  data:{event_category: 'Section', event_action: 'Click', event_name: 'Send me reset password instructions'}  %>
          </div>
        <% end %>
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</section>
<%= javascript_tag do %>
  $(document).ready(function(){
    $('section.login-page').css('height', $( window ).height() - $('section.login-page').offset().top );
    FormInitializer.init($("#password_form"));
  });
<% end %>
