<div class="card w-100">
  <div class="card-body">
    <p>
      Dear <%= @resource.name %>,
    </p>
    <div class="mb-3"></div>
    <p>Your account has been locked due to an excessive number of unsuccessful sign in attempts. Click the link below to unlock your account:</p>
    <div class="mb-3"></div>
    <%= link_to 'Unlock my account', unlock_url(@resource, unlock_token: @token) %>
  </div>
</div>
